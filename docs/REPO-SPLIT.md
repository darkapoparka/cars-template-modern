# Standalone repository split

This repository was created on 2026-09-10 from the live working tree at `J:/cars/templates/modern` while the parent `darkapoparka/cars` repository was at commit `adc16cc3e3ae430c1f53e64dd7086ce632e29e38`.

The source copy contained local working-tree changes in some templates. The split intentionally copied the live files, not merely the parent GitHub `main` snapshot, so current local template polish was preserved.

New canonical repository: `darkapoparka/cars-template-modern`.

The parent Cars repo remains the lead/orchestration workspace. This standalone repo owns future shared template improvements; dealer-specific personalization belongs in dealer copies/repositories.
