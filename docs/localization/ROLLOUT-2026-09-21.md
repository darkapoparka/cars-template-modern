# Modern portfolio rollout check — 21 September 2026

Starting main and fetched origin/main: `e8c1bca793b6350948f0a1b44d6e50b661ac0111`.
Native EN/BG rendering and mounted preference POST support are already committed.
No application source was changed during this check.

The localization policy integrity test still pinned the original candidate digest,
although reviewed commit `808d0367b4adfdaba2cd950b31ee5639a82c1d03` had hardened
normalized return paths against protocol-relative destinations. The test now pins
that exact hardened body (`5cac2494777341dfb0f2cbc886de13a554dee17bfa46ba8b116ddf57a2a700bc`).
The existing negative byte-drift assertion and save/dismiss JSON/form security
regressions remain enabled. This is a verification correction, not a rollback of
the security change or an application localization change.

Fresh checks on Node 22.23.2 and pnpm 11.4.0:

- `node --test scripts/localization-contract.test.mjs`: 8 passed.
- `pnpm --filter @repo/internationalization test`: 74 passed.
- `pnpm --filter web build`, with the documented demo environment,
  `NEXT_PUBLIC_BASE_PATH=/variant-2`, `AUTOMARKET_PUBLIC_E2E=true` and
  `E2E_PUBLIC_RUN_ID=locale-mounted-20260921`: passed, including TypeScript.

The build used the existing working tree, including 23 preserved unrelated
desktop draft paths. It is not exact committed-tree visual acceptance for those
drafts. This change commits only the integrity test and this receipt.

Port 6495 had no listener before the attempted production preview. Automatic
approval policy rejected its launch before execution. No server was started,
no existing server was stopped, and the rejected launch was not retried through
another mechanism. New mounted browser/no-JavaScript persistence acceptance was
therefore not obtained in this check.

Cars integration uses native `/variant-2/en/...` and `/variant-2/bg/...` routes,
build-time `NEXT_PUBLIC_BASE_PATH=/variant-2`, and
`/variant-2/api/preferences`. It must retain the native locale proxy/layout;
forcing the default locale is incompatible. This check does not deploy dealers,
change Cars release pins, or establish public-alias acceptance.
