/**
 * Stripe agent tooling is not part of the launch capability set. Keep the
 * public capability explicit without shipping the unused agent SDK and its
 * transitive runtime surface.
 */
export const paymentsAgentToolkit = undefined;
